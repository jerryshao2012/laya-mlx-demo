[project]
name = "laya-mlx-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jerryshao2012", email = "jerry.shao@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "laya-mlx>=0.2.0",
]

[project.scripts]
laya-mlx-demo = "laya_mlx_demo:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
